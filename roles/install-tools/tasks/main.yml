---

- name: Include variables file
  include_vars: variables.yml

- name: Granting sudo to all tasks in this block
  become: yes
  block:

    - name: Install Vim
      package:
        name: vim
        state: present

    - name: Install Curl
      package:
        name: curl
        state: present